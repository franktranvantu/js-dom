// Next, select all links inside the unordered list with the ID gallery and
// assign them to the variable galleryLinks.
let navigationLinks = document.querySelectorAll('nav a');
let galleryLinks = document.querySelectorAll('#gallery a');
let footerImages;